// OPERATORS!!

// Addition
int a = 1 + 1;  
int b = a + 2; // Adds the value of a to 2.
int c = a + b; // Adds both the value of b and a.

int sub1 = 2 - 4; 
int sub2 = sub1 - 5; // Substracts the value of sub1 by 5. 
int sub3 = sub1 - sub2; // Substract both the value of sub1 and sub2.

int multi1 = 2 * 2; 
int multi2 = multi1 * 4; // Multiplies multi1 by 4.
int multi3 = multi2 * multi1; // Multiplies both the value of multi1 and multi2.

int div1 = 8 / 2;
int div2 = div1 / 1; // Divides the value of div1 by 1.
int div3 = div1 / div2; // Divides both the value of div1 and div2.

int rem1 = 5 % 7; 
int rem2 = rem1 % 3; // Returns the remainder of rem1 and 3. 
int rem3 = rem2 % rem1; // Returns the remainder of rem2 and rem1.

int x = 0; 
int y = x++; // increments the value of x (increases the value of x).
int z = x--; // decrements the value of x (decreases the value of x).